import { Typography } from 'antd';

export default function DecisionTrace() {
  return (
    <div>
      <Typography.Title level={2}>Decision Trace</Typography.Title>
      <Typography.Text type="secondary">
        Single-transaction decision trace
      </Typography.Text>
    </div>
  );
}
